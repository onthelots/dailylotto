import 'package:flutter/material.dart';

class WeeklyRoundNumbers extends StatelessWidget {
  const WeeklyRoundNumbers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
